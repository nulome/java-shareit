server:
  port: 8080

logging:
  level:
    httpclient:
      wire: DEBUG
    org:
      springframework:
        web:
          client:
            RestTemplate: DEBUG
        apache:
          http: DEBUG

shareit-server:
  url: ${S_SERVER_URL:http://localhost:9090}

